class Distance:
    def __init__(self, p_digit, next_digit):
        self.distance = self.find(p_digit, next_digit)

    # TODO iterate through lists to find distance
    def find(self, p_digit, next_digit):
        return 0

    def get_distance(self):
        return self.distance
